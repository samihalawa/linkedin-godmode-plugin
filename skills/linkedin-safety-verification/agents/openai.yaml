interface:
  display_name: "LinkedIn Safety Verification"
  short_description: "Verify authorized LinkedIn batches without re-gating"
  default_prompt: "Use $linkedin-safety-verification to bind the authorized batch once, operate only through the selected authenticated controller, verify each write at the product layer, and continue without per-item confirmation."
policy:
  allow_implicit_invocation: true
