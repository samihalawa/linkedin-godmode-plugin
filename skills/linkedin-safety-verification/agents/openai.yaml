interface:
  display_name: "LinkedIn Safety Verification"
  short_description: "Verify LinkedIn batches without invented gates"
  default_prompt: "Use $linkedin-safety-verification to bind batch scope once, obey the selected controller's mandatory action-time boundary, verify each write at the product layer, and continue without agent-invented reconfirmation."
policy:
  allow_implicit_invocation: true
